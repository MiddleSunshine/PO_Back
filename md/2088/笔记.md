```c
#include <stdio.h>
#include <stdlib.h>

void ExitFun(void)
{
    printf("exit fun\n");
}

int main()
{
    atexit(ExitFun); // 注册退出处理函数
    printf("word"); // 如果没有清理缓冲区，将不会打印word
    exit(0);
}
```
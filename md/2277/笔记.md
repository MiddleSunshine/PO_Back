```c

struct spwd{
    char *sp_namp; // Login name / username
    char *sp_pwdp;// Encrypted password
    // 下面都是要求用户更新密码的一些时间参数
    long sp_lstchg;// 
    long sp_min;// 
    long sp_max;// 
    long sp_warn;// 
    long sp_inact;
    long sp_expire; // 账号过期时间
    unsigned long sp_flag; // 暂时无效
}

```
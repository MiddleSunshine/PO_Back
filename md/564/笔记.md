```js
onAssetCreate={async (app,file,id)=>{
                    let format = new FormData();
                    format.append("myfile", file);
                    return  await requestApi("OSS/index.php?method=Upload&bucket=oss-file-cache",
                        {
                            method: "post",
                            mode: "cors",
                            body: format
                        })
                        .then((res) => {
                            return res.json().then((json) => {
                                return json.Data.Url;
                            })
                        })
                }}
```